# Checkpoints

Only the TAU/DCASE checkpoint retained from the previous package is bundled here:

- `LightEchoNet_chpt_dcase2025.pth`

The ESC-50, UrbanSound8K, and FSD50K checkpoint files from the previous package are intentionally not included.

The bundled checkpoint is preserved unchanged. It is not renamed or rewritten to imply compatibility with the modified CP-Mobile student.
